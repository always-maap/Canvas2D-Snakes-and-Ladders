import { COLORS } from "./colors";

export const dark = {
  textColor: COLORS.white,
  backgroundColor: COLORS.dark,
  navBackgroundColor: COLORS.dark_purple,
};

export const light = {
  textColor: COLORS.dark,
  backgroundColor: COLORS.white_two,
  navBackgroundColor: COLORS.pink,
};
